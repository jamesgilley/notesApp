to run:
npm i && node index

https://github.com/jamesgilley/new

next:
create a notes object that contains an array 
of individual note objects with properties of note.noteText, note.noteTitle, and note.noteId.

display notes object or array at get(/api/notes)
with res.json({notes})

http://localhost:3000/api/notes
